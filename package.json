{
  "name": "mymath-project-2023-spring",
  "version": "1.0.0",
  "description": "my math ",
  "main": "server/dist/main.js",
  "scripts": {
    "postinstall": "cd client && yarn && yarn build && cd ../server && yarn && yarn build"
  },
  "engines": {
    "node": "16.10.0"
  },
  "cacheDirectories": [
    "client/node_modules",
    "server/node_modules"
  ]
}
